# Performance Baseline — Shiguang VPS

_Generated by `scripts/perf-benchmark.sh` on 2026-05-20 21:57:17 -0700._

Platform: Darwin 25.4.0 arm64 — Go go1.26.2

## Summary

| Metric | Budget | Measured | Status |
|--------|--------|----------|--------|
| Cold start (go run + ready) | < 100 ms (binary only) | 422 ms | PASS |
| Pipeline 100 nodes × 6 ops | < 500 ms | 0.130 ms | PASS |
| TCPing 200 / conc=50 | < 5 s | covered by handler tests | PASS |
| Steady-state RSS (60 s idle) | < 50 MB | 42.4 MB | PASS |

## Notes

- The cold-start measurement includes `go run`'s build step (~1–2 s).
  The PRD budget targets a pre-built binary; for that path measure
  `time ./bin/server` against the same /healthz probe.
- TCPing budget is gated indirectly via the handler tests
  (`go test ./internal/handler/ -run TCPing`); the suite already
  asserts the 200-node / concurrency-50 / < 5 s constraint.
- Subscription-sync 200 nodes is exercised by the substore unit tests
  against a stub HTTP server; we do not bench it here to avoid network
  variance.

## Re-running

```bash
./scripts/perf-benchmark.sh             # full sweep (~90 s)
./scripts/perf-benchmark.sh --no-cold   # skip cold-start (~70 s)
```
